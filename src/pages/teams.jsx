import styles from '../app/page.module.css'
import Layout from '@/components/layout';

const Teams = () => {
    return(
        <Layout>
            <h1>teams page</h1>
        </Layout>
    )
}

export default Teams;